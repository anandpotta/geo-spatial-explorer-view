
import { IMAGE_PATHS } from '@/lib/asset-paths';

const MyComponent = () => {
  return (
    <img 
      src={IMAGE_PATHS.ION_CREDIT} 
      alt="Ion Credit" 
      className="w-full h-auto"
    />
  );
};
